# SMTP Configuration for Email OTP
# Replace these with your actual SMTP provider credentials

# Gmail Example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="FarmConnect <no-reply@farmconnect.com>"

# OTP Settings
OTP_TTL_SECONDS=300
PORT=3001

# Supabase (if using)
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Instructions:
# 1. Copy this file to .env
# 2. For Gmail: Enable 2FA and create an App Password at https://myaccount.google.com/apppasswords
# 3. For other providers (SendGrid, Mailgun, etc.), use their SMTP credentials
